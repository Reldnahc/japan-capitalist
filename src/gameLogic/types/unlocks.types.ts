// Representation of a global unlock
export interface GlobalUnlock {
    description: string;  // Describes the global unlock (e.g., "Lemonade Stand: Revenue ×2")
    applied: boolean;     // Whether the unlock has been applied or not
}